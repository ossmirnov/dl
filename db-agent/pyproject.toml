[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "db-agent"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["retrieval*", "osint_agent*", "telegram_bot*"]
